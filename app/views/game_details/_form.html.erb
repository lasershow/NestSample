<%= form_for(@game_detail) do |f| %>
  <% if @game_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_detail.errors.count, "error") %> prohibited this game_detail from being saved:</h2>

      <ul>
      <% @game_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br>
    <%= f.text_field :game_id %>
  </div>
  <div class="field">
    <%= f.label :amazon %><br>
    <%= f.text_field :amazon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
